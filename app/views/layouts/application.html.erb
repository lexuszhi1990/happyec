<!DOCTYPE html>
<html>
<head>
  <title>English Corner</title>
  <link rel="shortcut icon" href="/images/e.png"/>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :all %>
  <%= csrf_meta_tag %>
  <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.2.6/jquery.min.js" type="text/javascript"></script>
  
</head>
<body>	



<div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
        <div class="container">
            <h3><a class="brand" href="/"><i class="icon-flag"></i>HAPPYEC</a></h3>
            <ul class="nav">

            </ul>
            <ul class="nav pull-right">
                  <% if current_user %>  
                    <% @user= User.find(current_user.id) %>
                    <li><%= link_to current_user.name, user_path(@user) %> </li>
                    <li><%= link_to "Log out", log_out_path %></li>
                  <% else %>  
                    <li><%= link_to "Log in", log_in_path %></li>
                  <% end %>  
            </ul>
        </div>
    </div>
</div>
<div id="container">
  <% flash.each do |name, msg| %>
  <%#FIXME: the close button does not work %>
    <div class="alert alert-<%= name == :notice ? "success" : "error" %>">
      <%= msg %>
    </div>
  <% end %>

<%= yield %>

</div><%# id="container" %>
<div class="footer">
	<a href="/users">Users</a><br \>
    <a href="http://www.ted.com">TED</a>
</div>
</body>
</html>

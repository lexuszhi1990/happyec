<!DOCTYPE html>
<html>
<head>
  <title>English Corner</title>
  <link rel="shortcut icon" href="/images/e.png"/>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= csrf_meta_tag %>
	<script type="text/javascript" src="http://cdn.sublimevideo.net/js/ubisak9n.js"></script>
</head>
<body>	

<% if current_user %>  <%# only myself can see this debug message %>
  <% @user= User.find(current_user.id) %>
  <% if @user.name == "happypeter"%>
    <% flash.each do |name, msg| %>
	  <%= content_tag :div, msg, :id => "flash_#{name}" %>
	<% end %>
  <%end%>
<%end%>

<table width="100%" id="pagetoptable">
	<tr>
		
		<td style="text-align:left;">
			<span class="pagetop">
			<%= link_to 'Home', events_path %>
			</span>
		</td>

		<td style="text-align:right;">
			<span class="pagetop">
				           
			<% if current_user %>  
			<% @user= User.find(current_user.id) %>
			<%= link_to current_user.name, user_path(@user) %> |
 			<%= link_to "Log out", log_out_path %>  
			<% else %>  
			<img src="/images/login.png" width=48 height=23></img> 
			<%= link_to "Log in", log_in_path %>  
			<% end %>  
			</span>
		</td>

	</tr>
</table>
<div id="container">

<%= yield %>

<div class="footer">
	<a href="/users">Users</a>
</div><%# id="container" %>
</body>
</html>
